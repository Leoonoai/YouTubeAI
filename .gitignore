# Raw learning materials — Mac local only, never sync to git
# 2.2 GB of mp4/mp3/pdf course materials, licenseScope: personal-use-only
# Lightweight transcripts/metadata go to .imoss/references/external/ instead
/raw-materials/

# Node / Python build artifacts
node_modules/
dist/
build/
*.pyc
__pycache__/
.venv/
venv/

# Local env / secrets
.env
.env.local
.env.*.local

# Editor / OS noise
.DS_Store
.vscode/
.idea/
*.swp
